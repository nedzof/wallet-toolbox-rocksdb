# refactor_rocksdb.md Implementation Audit

Date: 2026-05-16

Source objective: implement `/mnt/c/Users/nedzo/Documents/nektar-run/docs/refactor_rocksdb.md` for `wallet-toolbox-rocksdb`.

Scope correction applied after engineering review: this package remains a wallet library. Redis, BullMQ, SQLite, and NATS JetStream are not runtime dependencies here. Distributed queueing, lane fanout, proof queues, and NATS-backed coordination belong in Nektar/endurance/runtime infrastructure.

## Summary

The single-instance wallet-toolbox-owned work is implemented: in-memory UTXO/header caching, event-based invalidation, fresh provider checks before cache-informed storage mutations, parallel provider broadcasting with retry-priority backpressure and queue metrics, pooled HTTP clients with owner-aware lifecycle cleanup, active broadcast drain-on-close for both broadcast modes, RocksDB prefix indexes, schema columns, faster monitor scheduling with task resource teardown, service/cache event-listener teardown, wallet-level lifecycle cleanup, Prometheus metrics, architecture dependency checks, and focused tests.

The NATS JetStream deliverables from the source document are intentionally not implemented in this repo. They remain Nektar-owned; see `docs/nektar-runtime-handoff.md` for the runtime boundary and stream handoff.

## Completion Decision

Against the literal source objective, this goal is not complete: the source document requires a NATS JetStream cluster, NATS streams, consumers, TLS auth, NATS persistence tests, NATS latency metrics, and NATS-backed task coordination. Those are deliberately excluded from wallet-toolbox-rocksdb because this package must remain a wallet library, not an application runtime.

Against the accepted single-instance wallet-toolbox scope, the implementation is present and verified. The exact departures are intentional: provider connection pooling lives in the shared undici-backed HTTP adapter instead of inside `BroadcastOrchestrator`; monitor parallelism uses bounded async task loops instead of worker threads because monitor/storage/service objects are not worker-transferable runtime state; RocksDB write-buffer and level tuning are binding-limited by `@harperfast/rocksdb-js`, while the binding-supported parallelism, WAL, stats, block-cache, read-only, and transaction-log options are exposed.

## Prompt-To-Artifact Checklist

| Source requirement | Current artifact/evidence | Status |
| --- | --- | --- |
| Deploy 3-node NATS JetStream cluster | No NATS dependency. Enforced by `.dependency-cruiser.js` and `test/security/DependencyPolicy.test.ts`. | Nektar-owned, intentionally excluded |
| Configure `TX_BROADCAST`, `UTXO_STATUS`, `BLOCK_EVENTS`, `PROOF_REQUESTS` streams | No stream config in wallet-toolbox; ownership, subjects, payload contracts, adapter mapping, and suggested consumer settings are documented in `docs/nektar-runtime-handoff.md`. | Nektar-owned, intentionally excluded |
| Consumer groups, ack policies, TLS auth | No NATS consumers in wallet-toolbox. `docs/nektar-runtime-handoff.md` records durable group, explicit ack, max-deliver, ack-wait, max-ack-pending, TLS listener, least-privilege credential, and secrets-management guidance for Nektar. | Nektar-owned, intentionally excluded |
| Add runtime dependencies | `package.json` includes `lru-cache`, `node-cache`, `p-limit`, `p-queue`, `undici`, `prom-client`, and `eventemitter3`; `test/security/DependencyPolicy.test.ts` verifies `bullmq`, `ioredis`, `redis`, `nats`, `better-sqlite3`, and `sqlite3` are absent from manifests, installed lockfile packages, and runtime source imports. `p-limit` and `p-queue` use CommonJS-compatible majors because the requested v5/v8 packages are ESM-only while this package builds CommonJS output. | Implemented for single instance |
| Add dependency-cruiser dev dependencies | `package.json` includes `dependency-cruiser`. The requested `@types/dependency-cruiser` package is not installed because npm returns 404 for it, and `dependency-cruiser@16.10.4` already publishes `types/dependency-cruiser.d.mts`. | Implemented with available package |
| Dependency-cruiser architecture validation | `.dependency-cruiser.js` defines single-instance layer rules, bans Redis/BullMQ/NATS/SQLite runtime packages in `src`, keeps cache/event/broadcast/messaging helpers away from concrete runtime layers, `package.json` exposes `depcheck`, `depcheck:graph`, `depcheck:html`, and `depcheck:ci`, and `.github/workflows/push.yaml` runs `depcheck:ci` with a JSON summary in CI. `test/security/DependencyPolicy.test.ts` also checks root manifest fields and installed lockfile package entries for excluded runtimes. | Implemented |
| `src/cache/UtxoCacheManager.ts` with 10K entries and 30s TTL | `src/cache/UtxoCacheManager.ts`, defaults wired through `createDefaultWalletServicesOptions`; it refreshes LRU age on reads/has checks and supports source-doc-compatible read-hint helpers `getUtxoStatus(outpoint)` and `setUtxoStatus(outpoint, isUtxo, blockHeight)` on top of the cache-managed provider result shape. TTL coverage verifies stale true/false read hints expire and clean the outpoint index, and cache coverage verifies only decisive `isUtxo` boolean results are retained as read hints. | Implemented |
| `src/cache/BlockHeaderCache.ts` with 1000 entries and 5 minute TTL | `src/cache/BlockHeaderCache.ts`, defaults wired through `createDefaultWalletServicesOptions`; it uses `node-cache`, supports source-doc-compatible `getHeader(height)`, `setHeader(height, header)`, and `invalidateFromHeight(height)` methods in addition to hash-indexed lookups, rejects `setHeader` calls where the supplied height does not match the header metadata, and expiration coverage verifies TTL cleanup removes both height and hash indexes. | Implemented |
| L3 script hash cache | `src/cache/ScriptHashCache.ts`, wired through `Services.hashOutputScript` and `createDefaultWalletServicesOptions` with 10K entries and 5 minute TTL. | Implemented |
| Cache invalidation publisher | `src/messaging/publishers/CacheInvalidationPublisher.ts` publishes wallet-local block, UTXO invalidation, source-shaped UTXO status, and reorg events through `src/events/EventBus.ts`; cache managers consume those in-process events. It supports the source-doc convenience shapes `publishBlockInvalidation(blockHeight)`, `publishUtxoInvalidation(outpoints)`, `publishUtxoStatus(payload)`, and `publishReorg(payload)` without introducing NATS or treating cache status as spend authority. | Implemented for single instance |
| Cache listener lifecycle | Owned `UtxoCacheManager`, `BlockHeaderCache`, and `ScriptHashCache` instances expose `close()`. `EventBus` exposes typed `offBlockMined`, `offUtxoInvalidation`, and `offReorg` helpers so cache invalidation listeners can be removed through the same typed API used to subscribe. `Services.close()` tears down owned cache listeners and node-cache timers while preserving caller-injected cache ownership. | Implemented |
| Integrate cache with UTXO status lookups | `src/services/Services.ts` routes `getUtxoStatus` through cache unless fresh lookup is requested. | Implemented |
| Cache must not become spend authority | `confirmSpendableOutputs`, `TaskUnFail`, invalid-change `release`, and stale-input eviction use fresh provider checks before mutating or returning spend-authoritative state; read-only hints may use cache, while `Services.getUtxoStatus(..., useNext=true)` and the spend-authoritative `Services.isUtxo(output, true)` wrapper bypass cached hints. Stale-input eviction emits UTXO invalidation for the exact outpoints it marks unspendable. | Implemented |
| Output cache refresh metadata | Fresh provider checks that mutate output spendability stamp `cacheUpdatedAt` in invalid-change release, TaskUnFail reconciliation, and stale-input eviction. | Implemented |
| `src/broadcast/BroadcastOrchestrator.ts` | `src/broadcast/BroadcastOrchestrator.ts`. | Implemented |
| Parallel `Services.postBeef()` | `src/services/Services.ts` default `postBeefMode` is `PromiseAll`; same BEEF/txid set sent to providers, and provider exceptions are classified as service-error results without aborting other providers. `UntilSuccess` mode also records provider exceptions and continues failover. | Implemented |
| Record all provider broadcast attempts | `attemptToPostReqsToNetwork` stores aggregate history counts plus compact `providerAttempts` and `providerCount` fields on the outbox request history. | Implemented |
| Broadcast consumer | `src/messaging/consumers/BroadcastConsumer.ts` consumes already-signed in-process broadcast requests through `BroadcastOrchestrator` and preserves the same BEEF/txid semantics. `BroadcastRequest` supports both the internal `txids` array and the source-doc single `txid`/`rawTx` shape while still broadcasting the same BEEF, rejects ambiguous requests that supply both shapes, snapshots the normalized txid list before queueing so caller mutation cannot change provider inputs after validation, and rejects blank or whitespace-padded txids before provider dispatch. When `rawTx` is supplied, `BroadcastOrchestrator` rejects requests unless the raw transaction hashes to exactly one declared txid, enforcing the "one signed tx, one txid" invariant before broadcast. A NATS-backed durable consumer remains Nektar-owned. | Implemented for single instance |
| `TaskSendWaiting` publishes to NATS | `src/monitor/tasks/TaskSendWaiting.ts` uses bounded in-process queueing and direct storage processing. | Implemented for single instance |
| `TaskSendWaiting` chunk 500, trigger 1s | `src/monitor/tasks/TaskSendWaiting.ts`; unit coverage asserts default trigger, aged, quick-trigger, chunk, and process-concurrency settings. | Implemented |
| Broadcast retry priority/backpressure | `TaskSendWaiting` expands distinct batches with bounded parallel reads, then `processUnsent` uses bounded `p-queue` concurrency and prioritizes higher-attempt retry work before lower-attempt work with SendWaiting queue metrics. `BroadcastOrchestrator` also respects its configured concurrency cap, preserves p-queue priority when multiple signed broadcasts are queued behind active work, drains queued broadcasts on `close()`, and rejects new broadcasts after shutdown begins. | Implemented |
| Generic storage bulk read parallelism | `StorageProvider.findOutputsByIds`, `StorageProvider.findOutputsByOutpoints`, `findOrInsertOutputBasketsBulk`, `findOrInsertOutputTagsBulk`, `confirmSpendableOutputs`, the classification phase of `getReqsAndBeefToShareWithWorld`, and stale-input UTXO verification use bounded `p-limit` lookup fanout where set-based storage APIs are not available; duplicate basket/tag inputs are deduped before fanout, BEEF merge remains sequential, and `StorageKnex` keeps set-based SQL overrides. | Implemented |
| RocksDB prefix-based indexing | `src/storage/rocksdb/RocksDbWalletStore.ts` exposes prefix scans and indexed output lookups; indexed output resolution uses bounded parallel primary-record fetches and filters resolved primary records so stale secondary index records cannot return mismatched script-hash, spendable, or user-scoped outpoint outputs. | Implemented |
| RocksDB wallet storage provider | `src/storage/StorageRocksDb.ts` extends `StorageProvider`, is exported from `src/storage/index.all.ts`, persists wallet entities through `RocksDbWalletStore`, maintains output secondary indexes on insert/update, and supports `WalletStorageManager` active-store setup plus change allocation through provider methods. `test/storage/StorageRocksDb.test.ts` covers manager integration, provider-backed entity/change flows, and `Wallet.createAction` against a RocksDB-backed storage manager. | Implemented |
| Real-network testnet load test | `test/performance/TestnetThroughput.load.ts` is now SPV/paymail-only and delegates staged 10/50/100/500/1000 tx/s live runs to Nektar's circulation runner, preserving treasury sender, recipient paymail, source-lease, paymail receive, and unknown-outcome repair semantics. The old P2PKH/WhatsOnChain discovery and import path was removed from this harness. | Implemented |
| Secondary output index maintenance | `putOutput`, source-doc-compatible `updateOutput`, `deleteOutput`, and `rebuildOutputIndexes` maintain script hash, spendable, basket, and outpoint indexes. Lookup coverage includes stale-index filtering as an additional safety guard, including outpoint indexes that point across user ownership boundaries. `rebuildOutputIndexes` also backfills derived `scriptHash` metadata onto primary output records so recovery from legacy/raw primary records produces usable script-hash indexes. | Implemented |
| RocksDB `parallelismThreads=12` | `RocksDbWalletStore.defaultParallelismThreads = 12`. | Implemented |
| RocksDB `writeOptions` and `columnFamilies` tuning | Current `@harperfast/rocksdb-js@1.2.0` public `NativeDatabaseOptions` in `node_modules/@harperfast/rocksdb-js/dist/index.d.mts` and the local package README expose `parallelismThreads`, `disableWAL`, `enableStats`, `noBlockCache`, `pessimistic`, `readOnly`, `statsLevel`, and transaction-log options, plus global `blockCacheSize`/`compactOnClose`, but no `writeOptions`, `sync`, `compression`, `columnFamilies`, `writeBufferSize`, `maxWriteBufferNumber`, level-0 trigger, level-size, target-file-size, or compaction-byte options. `RocksDbWalletStoreOptions` exposes the supported DB/config options and rejects unsupported source-doc tuning knobs at runtime with `ROCKSDB_WALLET_STORE_UNSUPPORTED_OPTION:*` instead of silently pretending they applied. | Binding-limited with guard |
| Add `script_hash` column to outputs | `src/storage/schema/KnexMigrations.ts`; `TableOutput` metadata support. | Implemented |
| Extend ARC SSE for `BLOCK_MINED` | `src/monitor/tasks/TaskArcSSE.ts` handles block mined events and routes height/hash/outpoint notices through `Monitor.processBlockMinedNotice`. | Implemented |
| `src/chaintracker/SpvHeaderSync.ts` | `src/chaintracker/SpvHeaderSync.ts`; focused coverage verifies header/reorg event emission, cache invalidation, stop/unsubscribe cleanup, custom reorg handler fanout, idempotent `start()` subscription behavior, partial-start cleanup/retry behavior when one subscription succeeds before another fails, and `stop()` waiting for an in-flight startup before unsubscribing. | Implemented |
| Cache invalidation on block/reorg events | `EventBus`, `UtxoCacheManager`, `BlockHeaderCache`, `SpvHeaderSync`, and `TaskArcSSE`; `UtxoCacheManager` clears cached read hints on block and reorg events, can use block-event `outpoints` for targeted invalidation when a runtime bridge has computed affected outputs, and falls back to full cache invalidation when block contents are unknown. `BlockHeaderCache` invalidates cached headers from height-only block events, replaces headers from full header events, and invalidates deactivated headers on reorg. `Services.getHeaderForHeight()` and `Services.hashToHeader()` validate returned headers match the requested height/hash before caching. `SpvHeaderSync` emits reorg events even when a custom reorg handler is registered. Integration coverage proves `Monitor.processNewBlockHeader()` updates the shared `Services.blockHeaderCache` and clears `Services.utxoCache` through the shared event bus, and publisher coverage proves reorg bridge events clear UTXO hints while invalidating deactivated block headers. | Implemented |
| Block event publishing to NATS | No NATS in wallet-toolbox. | Nektar-owned, intentionally excluded |
| Parallel monitor task execution | `src/monitor/Monitor.ts` uses per-task async scheduler loops and bounded `p-limit`; focused tests prove due tasks run in parallel and `taskRunConcurrency` caps simultaneous task execution. | Implemented |
| Worker-based parallelism | Replaced with bounded async task loops to preserve shared monitor/storage/service state without worker serialization. | Implemented by safer local design |
| Faster task configuration | `Monitor` and `TaskSendWaiting` defaults reduced to 1s where applicable; height-only block notices from SSE wake `TaskCheckForProofs`. | Implemented |
| Monitor task resource cleanup | `WalletMonitorTask.asyncDestroy`, `TaskArcadeSSE.asyncDestroy`, and `Monitor.destroy()` close realtime subscriptions and task-owned resources. | Implemented |
| Wallet lifecycle cleanup | `Wallet.destroy()` destroys monitor resources, closes service pools, then destroys storage and privileged key state. | Implemented |
| NATS-based task coordination | No NATS in wallet-toolbox. `docs/nektar-runtime-handoff.md` records Nektar-owned runtime task coordination rules for `TX_BROADCAST`, `PROOF_REQUESTS`, `BLOCK_EVENTS`, and `UTXO_STATUS`, including durable-consumer work leases, idempotency-before-side-effects, ack-after-wallet-update, and disabling overlapping local monitor ownership in Nektar deployments. | Nektar-owned, intentionally excluded |
| Replace native provider fetch with undici Pool | `src/http/UndiciHttpClient.ts`; ARC, Bitails, WhatsOnChain, exchange rates, chaintracks service clients, and the P2PKH BEEF-building helper default to pooled undici-backed HTTP. | Implemented |
| Replace all runtime native `fetch()` calls | `test/security/HttpClientPolicy.test.ts` rejects bare native `fetch()` in runtime TypeScript. `test/fundWalletP2PKH.test.ts` verifies BEEF-building uses an injected/default HTTP client instead of global fetch. Remaining `authClient.fetch(...)` calls are authenticated BSV SDK flows, not native provider HTTP. | Implemented |
| Connection pooling 50 connections, pipelining 10 | `UndiciHttpClient` defaults and `createDefaultWalletServicesOptions`. | Implemented |
| HTTP pool lifecycle | `src/services/Services.ts` exposes `close()` and closes string/default-options clients it owns while preserving caller-injected clients. `UndiciHttpClient` reuses one undici `Pool` per origin and clears owned pools on `close()`. `Services.close()` waits for active `postBeef` operations and the internal postBeef queue to drain before closing owned resources, and rejects new broadcasts after shutdown starts. | Implemented |
| API/config surface | `WalletServicesOptions` exposes cache sizes/TTLs, shared event bus, metrics, shared pooled HTTP client, default pooled HTTP client options, postBeef mode, postBeef soft timeouts, and postBeef queue concurrency. Services-level coverage verifies owned UTXO, block-header, and script-hash caches actually apply configured size and TTL values. `MonitorOptions.broadcastConfig` tunes `TaskSendWaiting` chunk size and processing concurrency; provider connection-pool sizing remains owned by the shared Services HTTP client. `RocksDbWalletStoreOptions` exposes supported RocksDB tuning options. `natsConfig` and unsupported RocksDB `writeBufferSize` are intentionally absent. | Implemented for wallet-owned knobs |
| Remove SQLite runtime surface | `better-sqlite3` is removed from root dependencies, `Setup.createSQLiteKnex`, `Setup.createWalletSQLite`, `SetupWalletSQLiteArgs`, `SetupEnv.filePath`, `MonitorDaemon.sqliteFilename`, SQLite `DBType`, SQLite PRAGMA handling, and Chaintracks' implicit SQLite-file default are removed. SQLite-backed test helpers, SQLite-backed tests, checked-in SQLite fixtures, and stale generated `out/` artifacts were removed. `test/security/DependencyPolicy.test.ts` and `.dependency-cruiser.js` ban direct SQLite runtime dependencies, the policy test rejects installed `node_modules/better-sqlite3` or `node_modules/sqlite3` lockfile entries, scans first-party files for SQLite setup/documentation/fixture reintroduction outside policy/audit evidence, and verifies the remaining lockfile SQLite strings are only Knex optional peer metadata. | Implemented |
| Service provider HTTP client shape | Providers keep the SDK `HttpClient` abstraction and receive a shared undici-backed adapter instead of depending directly on `Pool`, preserving SDK compatibility while providing pooled provider HTTP. | Implemented by safer local design |
| Data-flow diagrams | Wallet-toolbox implements the local equivalents through `Services.postBeef`, `BroadcastOrchestrator`, `EventBus`, cache managers, `TaskArcSSE`, `SpvHeaderSync`, and monitor/storage APIs. Nektar-owned NATS flow mappings for broadcast, UTXO invalidation, block events, and proof requests are documented in `docs/nektar-runtime-handoff.md`. | Implemented / Nektar-owned split |
| Performance projections and resource requirements | The local mocked throughput harness verifies the single-instance broadcast queue can exceed 1000 mocked calls/s with bounded concurrency and now reports `mode: "mocked-in-process"` plus the real I/O it does not cover. `npm run benchmark:rocksdb-indexes` benchmarks indexed RocksDB script-hash, spendable, basket, and outpoint lookups against seeded wallet outputs. CPU, memory, disk, network, JetStream storage, and public-network finality targets are Nektar deployment validation concerns documented in `docs/nektar-runtime-handoff.md` and the real-network next phase is captured in `docs/practical-testing-plan.md`. | Partially implemented; deployment-owned |
| Migration strategy phases | Cache, broadcast, storage, real-time sync, monitor redesign, focused testing, dependency checks, and documentation updates are implemented for the wallet-owned single-instance scope. Nektar infrastructure, stream rollout, runtime worker ownership, throughput ladder, failure scenarios, and resource validation are documented as runtime rollout gates in `docs/nektar-runtime-handoff.md`. | Implemented / Nektar-owned split |
| Prometheus metrics | `src/metrics/WalletToolboxMetrics.ts`, `src/metrics/PrometheusMetrics.ts`. | Implemented |
| Broadcast latency metrics | `WalletToolboxMetrics.recordPostBeefProvider`. | Implemented |
| Broadcast backlog metrics | `WalletToolboxMetrics` exposes provider and SendWaiting queue gauges; `TaskSendWaiting` updates its queue gauges. | Implemented |
| Cache hit metrics | `WalletToolboxMetrics.recordUtxoCacheRequest` and block header equivalents export request counters, entry gauges, and direct hit-rate gauges; `UtxoCacheManager`, `BlockHeaderCache`, and `ScriptHashCache` expose local hit/miss/hit-rate stats. | Implemented |
| Storage query latency metrics | `WalletToolboxMetrics.recordStorageQuery`; `RocksDbWalletStore` wraps get, put, delete, batch, scan, and compact operations with storage query timing. | Implemented |
| NATS latency metrics | No NATS in wallet-toolbox. `docs/nektar-runtime-handoff.md` records suggested Nektar-owned NATS latency, backlog, redelivery, ack-failure, publish-failure, and worker-processing metrics plus alert conditions. | Nektar-owned, intentionally excluded |
| Prometheus endpoint | `src/storage/adminServer/adminServer.ts` exposes metrics before admin auth; endpoint tests cover UTXO and block-header counters, entry gauges, and direct hit-rate gauges. | Implemented |
| Alerting rules | `docs/prometheus-alerts.yml` covers low UTXO cache hit rate, low block-header cache hit rate, provider latency, broadcast queue backlog, SendWaiting backlog, and storage query latency. Cache alerts intentionally derive recent-window hit rates from request counters instead of using process-lifetime hit-rate gauges. Alert coverage verifies all wallet-local rules preserve `expr`, `for`, severity, summary, and description fields while keeping NATS/JetStream alerts out of the wallet-toolbox artifact. | Implemented for single instance |
| Rate limiting security consideration | `src/security/RateLimiter.ts`; `StorageServer` can enforce per-identity authenticated request limits through `rateLimit` options. | Implemented for storage remoting |
| `was_broadcast`, `rebroadcast_attempts` columns | `src/storage/schema/KnexMigrations.ts`; proven tx req table support. The persisted property names follow the existing wallet schema's camelCase convention (`wasBroadcast`, `rebroadcastAttempts`), and `test/storage/EntityProvenTxReq.rebroadcast.test.ts` verifies the proof-timeout circuit breaker uses them to rebroadcast only previously broadcast transactions, stop at the configured limit, and forward both false/zero and true/count values through dynamic storage updates. | Implemented |
| `script_hash`, `cache_updated_at` columns | `src/storage/schema/KnexMigrations.ts`; output table support. | Implemented |
| Migration script | `src/storage/schema/KnexMigrations.ts`. | Implemented |
| Load test to 1000 tx/s | `npm run loadtest:single-instance` passed on 2026-05-16 with 1000 calls, concurrency 100, peak active broadcasts 100, and observed about 37.4k mocked TPS after the SQLite cleanup and matrix refresh. This is explicitly a mocked in-process queue/concurrency harness, not proof of real-network 1000 tx/s. | Implemented as mocked harness |
| Benchmark query performance | `test/performance/RocksDbIndexBenchmark.load.ts` and `npm run benchmark:rocksdb-indexes` seed RocksDB outputs, exercise prefix-indexed script-hash, spendable, basket, and outpoint lookups, assert result counts, and print per-query timings without relying on a flaky default time threshold. | Implemented as local harness |
| Failure scenario testing | `test/broadcast/BroadcastOrchestrator.test.ts` verifies provider failure rejects only the failed broadcast while later queued signed work still runs and the queue drains cleanly; close-drain, retry priority, cache invalidation, reorg, stale-input, rate-limit, and dependency-policy failure scenarios have focused coverage. | Implemented for single instance |
| Test cache invalidation | `test/cache/CacheManagers.test.ts`, `test/services/Services.cache.test.ts`, stale-input invalidation tests, and the in-memory stale-input concurrency test. | Implemented |
| Test NATS persistence | No NATS in wallet-toolbox. `docs/nektar-runtime-handoff.md` records a Nektar runtime acceptance-test matrix for durable broadcast messages, ack redelivery, duplicate idempotency, quorum loss, snapshot restore, TLS rejection, schema quarantine, and reorg adapter behavior. | Nektar-owned, intentionally excluded |
| Nektar handoff contract drift guard | `test/docs/NektarRuntimeHandoff.test.ts` asserts the handoff doc preserves the four stream contracts, subjects, idempotency and ack rules, adapter mappings, runtime NATS metrics, runtime acceptance gates, stream retention/replica/dedupe settings, TLS/security guidance, object-storage restore guidance, staged throughput gates, public-network validation checks, and no-cache-authority/no-replacement-transaction boundary rules without adding a NATS dependency to wallet-toolbox. | Implemented as documentation contract |
| Test reorg handling | `test/chaintracker/SpvHeaderSync.test.ts`, `test/monitor/Monitor.parallel.test.ts`, cache tests. | Implemented |
| Validate SPV sync accuracy | `test/chaintracker/SpvHeaderSync.test.ts` and Services cache coverage verify event fanout, lifecycle cleanup, reorg invalidation, and header height/hash mismatch rejection before cache insertion. | Implemented at unit/integration level |
| Disaster recovery: NATS persistence and snapshots | No NATS in wallet-toolbox. `docs/nektar-runtime-handoff.md` records suggested JetStream retention, file-storage, replica, duplicate-window, object-storage snapshot, restore-test, backlog, redelivery, and storage-pressure guidance for Nektar operators. | Nektar-owned, intentionally excluded |
| Disaster recovery: RocksDB snapshots | `RocksDbWalletStore` exposes `flush()`, `compact()`, `compactOnClose`, and `prepareForFilesystemSnapshot()` for operator-owned snapshot handoff, and `docs/rocksdb.md` documents the quiesce/flush/compact/platform-snapshot/restore-test flow. The current `@harperfast/rocksdb-js` binding does not expose a checkpoint/backup API, so filesystem snapshots remain operator-owned. | Operator-owned / binding-limited |
| Disaster recovery: cache backup | Caches are in-memory read accelerators and are rebuildable from provider/storage state. | Implemented by design |
| Configuration backup | Wallet-toolbox exposes library-level options only. Nektar-owned runtime configuration should be Git versioned with environment-specific overlays for stream limits, consumers, TLS credential references, worker concurrency, provider pools, and alert thresholds, as documented in `docs/nektar-runtime-handoff.md`. | Nektar-owned |

## Principal Review Fact-Check

| Review point | Verified state | Action |
| --- | --- | --- |
| Mocked load test does not prove production 1000 tx/s | Correct. `test/performance/SingleInstanceThroughput.load.ts` uses a mocked `postBeef` with a queue yield and no RocksDB/provider/BEEF/finality I/O. | Load-test output now labels itself as `mocked-in-process`; `docs/practical-testing-plan.md` records the real-network testnet phase and required bottleneck checks. |
| `src/messaging/` is empty | Incorrect in this checkout. `src/messaging/consumers/BroadcastConsumer.ts`, `src/messaging/publishers/CacheInvalidationPublisher.ts`, and their index files exist and are exported through `src/index.all.ts`. | No code change needed; named-file checklist keeps these artifacts explicit. |
| `better-sqlite3` is still a runtime dependency | Incorrect in this checkout. `package.json` has no `better-sqlite3` or `sqlite3` dependency; remaining lockfile strings are Knex optional peer metadata verified by `test/security/DependencyPolicy.test.ts`. | No dependency change needed. |
| 15 circular dependency warnings remain | Correct. `npm run depcheck` reports 0 errors and the same 15 circular warnings. | Kept as non-blocking structural debt; resolving them is outside this focused performance architecture implementation. |
| RocksDB advanced tuning is binding-limited | Correct. `@harperfast/rocksdb-js` does not expose the requested write-buffer/column-family/compaction knobs. | Runtime guard rejects unsupported tuning knobs and docs keep this as binding-limited. |
| Next phase should be practical testnet validation | Correct. | Added `docs/practical-testing-plan.md` and drift coverage in `test/docs/PracticalTestingPlan.test.ts`. |

## Named File Checklist

| Source-named file | Current artifact/evidence | Status |
| --- | --- | --- |
| `package.json` | Runtime dependencies include `lru-cache`, `node-cache`, `p-limit`, `p-queue`, `undici`, `prom-client`, and `eventemitter3`; dependency-cruiser, broadcast load-test, and RocksDB index benchmark scripts are present; Redis/BullMQ/NATS/SQLite runtime packages are absent by policy. | Implemented for single instance |
| `src/cache/UtxoCacheManager.ts` | Implements the 10K-entry/30s default LRU UTXO read-hint cache, source-compatible `getUtxoStatus`/`setUtxoStatus` helpers, in-flight request coalescing, targeted outpoint invalidation, block/reorg invalidation, decisive-result-only caching, stats, metrics hooks, and listener cleanup. | Implemented |
| `src/cache/BlockHeaderCache.ts` | Implements the 1000-entry/5 minute default block-header cache using `node-cache`, height/hash indexes, source-compatible `getHeader`/`setHeader`/`invalidateFromHeight`, mismatch rejection, block/reorg invalidation, stats, metrics hooks, and timer/listener cleanup. | Implemented |
| `src/messaging/publishers/CacheInvalidationPublisher.ts` | Publishes wallet-local block, UTXO, UTXO status, and reorg invalidation signals through `EventBus` rather than NATS, preserving the source adapter shapes for Nektar bridges. | Implemented for single instance |
| `src/broadcast/BroadcastOrchestrator.ts` | Wraps already-signed BEEF broadcasts in a bounded priority queue, preserves same-BEEF/same-txid semantics, validates raw tx/txid consistency, snapshots txids before queued execution, rejects ambiguous/blank/whitespace-padded txid shapes, drains on close, and leaves provider HTTP pooling in shared Services clients. | Implemented for single instance |
| `src/messaging/consumers/BroadcastConsumer.ts` | Provides an in-process adapter for already-signed broadcast requests and delegates to `BroadcastOrchestrator`; durable NATS consumer groups remain Nektar-owned. | Implemented for single instance |
| `src/messaging/NatsManager.ts` | Not present in wallet-toolbox by design. The NATS manager, cluster connection, stream creation, consumer groups, TLS credentials, and persistence operations are documented as Nektar/endurance/runtime responsibilities in `docs/nektar-runtime-handoff.md` and are excluded by dependency policy. | Nektar-owned, intentionally excluded |
| `src/http/UndiciHttpClient.ts` | Provides the pooled undici-backed SDK `HttpClient` adapter with one pool per origin, configurable default pool sizing, and close/cleanup semantics. | Implemented |
| `src/chaintracker/SpvHeaderSync.ts` | Subscribes to chaintracker header/reorg events, emits wallet-local block/reorg signals, triggers proof/reorg paths, handles idempotent start/stop lifecycle, and cleans up partial startup failures. | Implemented |
| `src/metrics/PrometheusMetrics.ts` | Exposes the Prometheus registry-backed metrics implementation used by wallet-toolbox cache, broadcast, queue, and storage telemetry. NATS runtime metrics remain documented for Nektar. | Implemented for single instance |
| `src/storage/rocksdb/RocksDbWalletStore.ts` | Implements prefix scans, script-hash and spendable output indexes, source-compatible `updateOutput`, stale secondary-index filtering, `parallelismThreads=12`, supported RocksDB config passthrough, runtime rejection for unsupported source-doc RocksDB tuning knobs, storage metrics, and snapshot-preparation handoff. | Implemented; advanced tuning binding-limited |
| `src/services/Services.ts` | Owns in-memory UTXO/header/script-hash caches, routes UTXO lookups through cache with fresh-check bypass, defaults `postBeefMode` to `PromiseAll`, queues provider broadcasts, records metrics, and drains owned cache/HTTP resources on `close()`. | Implemented |
| `src/services/providers/ARC.ts` | Uses SDK `HttpClient` backed by `createUndiciHttpClient()` when no client is injected, preserving provider API compatibility while routing runtime HTTP through pooled undici. | Implemented by adapter |
| `src/services/providers/WhatsOnChain.ts` | Uses the shared Services `HttpClient` abstraction for provider calls; `test/security/HttpClientPolicy.test.ts` bans bare native `fetch()` in runtime provider HTTP. | Implemented by adapter |
| `src/services/providers/Bitails.ts` | Uses SDK `HttpClient` backed by `createUndiciHttpClient()` when no client is injected. | Implemented by adapter |
| `src/monitor/tasks/TaskSendWaiting.ts` | Defaults to 1s trigger/aged intervals, `chunkLimit=500`, bounded p-limit batch expansion, p-queue processing concurrency, retry priority, and SendWaiting queue metrics. It remains in-process instead of publishing to NATS. | Implemented for single instance |
| `src/monitor/tasks/TaskArcSSE.ts` | Handles `BLOCK_MINED`, calls `Monitor.processBlockMinedNotice`, and cleans up realtime resources in `asyncDestroy()`. | Implemented |
| `src/monitor/Monitor.ts` | Wires `SpvHeaderSync`, emits block/reorg events, schedules monitor tasks with bounded async loops, exposes `broadcastConfig` for SendWaiting chunk/concurrency tuning, and destroys task-owned resources. NATS coordination remains Nektar-owned. | Implemented for single instance |
| `src/storage/StorageProvider.ts` | Adds bounded p-limit fanout for generic bulk reads, request classification, and `confirmSpendableOutputs`; keeps SQL set-based overrides in `StorageKnex`. | Implemented |
| `src/storage/methods/attemptToPostReqsToNetwork.ts` | Records per-provider attempt history, tracks broadcast/rebroadcast state, uses bounded stale-input UTXO checks, stamps `cacheUpdatedAt`, and emits exact UTXO invalidation events for stale inputs. | Implemented |
| Additional guard artifacts | `src/events/EventBus.ts`, `src/cache/ScriptHashCache.ts`, `docs/prometheus-alerts.yml`, `.dependency-cruiser.js`, `docs/nektar-runtime-handoff.md`, and the security/dependency tests cover the event bus, L3 cache, alerting, architecture enforcement, and runtime-boundary documentation implied by the source architecture. | Implemented / Nektar-owned split |

## Latest Verification

- Final spot-check on 2026-05-16: added `SpvHeaderSync.start()` idempotency coverage proving repeated concurrent starts reuse the same subscriptions and that a stop/start cycle resubscribes cleanly; `timeout 180s npm run build`, `timeout 120s npx jest test/chaintracker/SpvHeaderSync.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 4 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added `BroadcastOrchestrator` concurrency-cap coverage proving queued broadcasts can run in parallel but never exceed the configured queue concurrency; `timeout 180s npm run build`, `timeout 120s npx jest test/broadcast/BroadcastOrchestrator.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 6 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: expanded `CacheInvalidationPublisher` coverage to verify `publishReorg()` bridges reorg events to cache listeners, clearing UTXO read hints and invalidating deactivated block headers without clearing unaffected headers; `timeout 180s npm run build`, `timeout 120s npx jest test/messaging/CacheInvalidationPublisher.test.ts test/cache/CacheManagers.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 15 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: expanded `test/storage/EntityProvenTxReq.rebroadcast.test.ts` to verify `updateStorageDynamicProperties()` forwards `wasBroadcast` and `rebroadcastAttempts` through the dynamic storage update path, including false/zero values; `timeout 180s npm run build`, `timeout 120s npx jest test/storage/EntityProvenTxReq.rebroadcast.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 3 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: strengthened Prometheus alert coverage so `test/metrics/PrometheusAlerts.test.ts` verifies every wallet-local alert rule has an expression, duration, severity label, summary, and description, and that NATS/JetStream alerting remains outside the wallet-toolbox single-instance alert file; `timeout 180s npm run build`, `timeout 120s npx jest test/metrics/PrometheusAlerts.test.ts test/metrics/WalletToolboxMetrics.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 2 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: hardened the source-doc broadcast request shape by validating any supplied `rawTx` against exactly one declared `txid` before queueing the signed BEEF for broadcast; `timeout 180s npm run build`, `timeout 120s npx jest test/broadcast/BroadcastOrchestrator.test.ts test/messaging/BroadcastConsumer.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 8 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added Services-level cache option wiring coverage proving custom owned UTXO, block-header, and script-hash cache max/TTL options are applied by the runtime Services constructor; `timeout 180s npm run build`, `timeout 120s npx jest test/services/Services.cache.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 8 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added entity-level rebroadcast-state coverage for the source doc's `was_broadcast`/`rebroadcast_attempts` schema change; `timeout 180s npm run build`, `timeout 120s npx jest test/storage/EntityProvenTxReq.rebroadcast.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 2 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added monitor scheduling coverage proving `Monitor.runOnce()` respects the configured `taskRunConcurrency` cap when more tasks are due than may run concurrently; `timeout 180s npm run build`, `timeout 120s npx jest test/monitor/Monitor.parallel.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 8 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added `UtxoCacheManager` TTL-expiration coverage proving stale outpoint status hints, including cached `false` statuses, expire to `null` and clean the outpoint index; `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 13 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added `BlockHeaderCache` TTL-expiration coverage proving node-cache expiry removes both height and hash indexes and updates the entry count; `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 12 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: strengthened `RocksDbWalletStore.rebuildOutputIndexes()` so index recovery rewrites primary outputs with derived script-hash metadata before rebuilding secondary indexes; `timeout 180s npm run build`, `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand`, `timeout 180s npm run benchmark:rocksdb-indexes`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 11 tests. The benchmark seeded 1000 outputs, checked 100 outpoints, reported script-hash query 9.834ms, spendable query 26.793ms, basket query 3.131ms, outpoint query 5.483ms, and validated result counts `{ scriptHash: 63, spendable: 500, basket: 125, outpoint: 100 }`; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added `test/docs/NektarRuntimeHandoff.test.ts` so the Nektar-owned NATS handoff cannot drift away from the required stream contracts, subjects, idempotency/ack rules, adapter mappings, runtime metrics, and acceptance gates while keeping NATS out of wallet-toolbox runtime code; `timeout 180s npm run build`, `timeout 120s npx jest test/docs/NektarRuntimeHandoff.test.ts test/security/DependencyPolicy.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 7 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: strengthened SQLite removal enforcement so `test/security/DependencyPolicy.test.ts` now scans first-party files for SQLite surface outside the narrow policy/audit allowlist and asserts `package-lock.json` SQLite strings are limited to Knex optional peer metadata; `timeout 180s npm run build`, `timeout 120s npx jest test/security/DependencyPolicy.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 5 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings. A focused SQLite grep found only policy/audit references plus Knex optional peer metadata, `npm ls better-sqlite3 sqlite3 --depth=0` showed `(empty)`, and the SQLite fixture filename scan returned no files.
- Final spot-check on 2026-05-16: added typed `EventBus` unsubscribe helpers for block, UTXO-invalidation, and reorg events, with lifecycle coverage proving listeners are removed and no longer receive cache invalidation events; `timeout 180s npm run build`, `timeout 120s npx jest test/events/EventBus.test.ts test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 3 suites and 19 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added HTTP pool lifecycle coverage proving `UndiciHttpClient` reuses one undici `Pool` per origin and clears the pool map on close; `timeout 180s npm run build`, `timeout 120s npx jest test/services/UndiciHttpClient.test.ts test/services/ProviderHttpClientDefaults.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 8 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added a runtime guard for source-doc RocksDB tuning knobs unsupported by the current `@harperfast/rocksdb-js` binding (`writeOptions`, `columnFamilies`, `writeBufferSize`, level-0/level-size/compaction options, `compression`, and `sync`) so JS callers get a clear `ROCKSDB_WALLET_STORE_UNSUPPORTED_OPTION:*` error instead of a silent no-op; `timeout 180s npm run build`, `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 10 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added `npm run benchmark:rocksdb-indexes` and `test/performance/RocksDbIndexBenchmark.load.ts` for the source doc's RocksDB query-performance benchmark gate; `timeout 180s npm run benchmark:rocksdb-indexes`, `timeout 120s npm run depcheck`, and `git diff --check` passed. The benchmark seeded 1000 outputs, checked 100 outpoints, reported script-hash query 10.813ms, spendable query 20.275ms, basket query 3.997ms, outpoint query 5.712ms, and validated result counts `{ scriptHash: 63, spendable: 500, basket: 125, outpoint: 100 }`; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added single-instance broadcast failure coverage proving provider failure rejects only the failed queued broadcast and does not poison later signed work; `timeout 180s npm run build`, `timeout 120s npx jest test/broadcast/BroadcastOrchestrator.test.ts test/messaging/BroadcastConsumer.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 7 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: aligned `UtxoCacheManager` with the source LRU behavior by enabling `updateAgeOnGet`/`updateAgeOnHas` and adding TTL refresh coverage for hot read hints; `timeout 180s npm run build`, `timeout 90s npx jest test/cache/CacheManagers.test.ts --runInBand`, and `timeout 120s npm run depcheck` passed. Jest reported 1 suite and 11 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added `CacheInvalidationPublisher.publishUtxoStatus(payload)` so Nektar `UTXO_STATUS` adapter payloads can be bridged into wallet-local cache invalidation without copying `isUtxo` into spendability state; `timeout 180s npm run build`, `timeout 90s npx jest test/messaging/CacheInvalidationPublisher.test.ts test/cache/CacheManagers.test.ts --runInBand`, and `timeout 120s npm run depcheck` passed. Jest reported 2 suites and 11 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: tightened the Nektar-owned `BLOCK_EVENTS` handoff contract so `block.mined` payloads may carry optional affected wallet `outpoints`, with adapter mapping to `Monitor.processBlockMinedNotice(height, hash, header, outpoints)`, `CacheInvalidationPublisher.publishBlockInvalidation(height, outpoints)`, or `EventBus.emitBlockMined({ ..., outpoints })` for targeted wallet-local cache invalidation.
- Final spot-check on 2026-05-16: `BlockHeaderCache` now invalidates cached headers from height-only block events and replaces headers from full header block events, matching the source real-time sync invalidation path; `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts test/monitor/Monitor.parallel.test.ts test/chaintracker/SpvHeaderSync.test.ts src/monitor/tasks/__tests/TaskArcSSE.test.ts --runInBand`, and `timeout 120s npm run depcheck` passed. Jest reported 4 suites and 44 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: `TaskArcadeSSE` now forwards optional `outpoints` from `BLOCK_MINED` SSE payloads through `Monitor.processBlockMinedNotice(height, hash, header, outpoints)` for targeted wallet-local cache invalidation; `timeout 180s npm run build`, `timeout 120s npx jest src/monitor/tasks/__tests/TaskArcSSE.test.ts test/cache/CacheManagers.test.ts test/monitor/Monitor.parallel.test.ts --runInBand`, and `timeout 120s npm run depcheck` passed. Jest reported 3 suites and 40 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: expanded current wallet-owned architecture matrix passed with `timeout 300s npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts test/broadcast/BroadcastOrchestrator.test.ts test/services/Services.postBeefTimeouts.test.ts test/storage/RocksDbWalletStore.test.ts test/storage/StorageProvider.bulkReads.test.ts test/storage/attemptToPostReqsToNetwork.history.test.ts test/storage/confirmSpendableOutputs.test.ts test/storage/markStaleInputsAsSpent.concurrent.test.ts src/monitor/tasks/__tests/TaskSendWaiting.test.ts src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts test/metrics/WalletToolboxMetrics.test.ts test/metrics/PrometheusAlerts.test.ts test/chaintracker/SpvHeaderSync.test.ts test/monitor/Monitor.parallel.test.ts src/monitor/tasks/__tests/TaskArcSSE.test.ts test/security/DependencyPolicy.test.ts test/security/HttpClientPolicy.test.ts test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts test/messaging/BroadcastConsumer.test.ts test/messaging/CacheInvalidationPublisher.test.ts test/storage/adminServer/AdminServer.metrics.test.ts test/fundWalletP2PKH.test.ts test/security/RateLimiter.test.ts test/storage/remoting/StorageServer.rateLimit.test.ts --runInBand`: 26 suites, 111 tests.
- Final spot-check on 2026-05-16: current single-instance load harness `timeout 180s npm run loadtest:single-instance` passed with 1000 calls, concurrency 100, `maxActive` 100, and about 37.4k mocked TPS; `timeout 120s npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: expanded `docs/nektar-runtime-handoff.md` with runtime rollout gates, throughput-ladder guidance, public-network validation requirements, deployment resource ownership, and Git-versioned runtime configuration guidance for source sections 10, 11, 13, and 16.
- Final spot-check on 2026-05-16: after adding optional block-event `outpoints` to support targeted UTXO cache invalidation while preserving full-clear fallback, `timeout 180s npm run build` and `timeout 120s npx jest test/cache/CacheManagers.test.ts test/messaging/CacheInvalidationPublisher.test.ts test/monitor/Monitor.parallel.test.ts test/chaintracker/SpvHeaderSync.test.ts --runInBand` passed; Jest reported 4 suites and 20 tests.
- Final spot-check on 2026-05-16: exposed the targeted block-invalidation outpoint path through `Monitor.processBlockMinedNotice(height, hash, header, outpoints)` and `CacheInvalidationPublisher.publishBlockInvalidation(height, outpoints)` so Nektar/runtime adapters can bridge precomputed affected outpoints into wallet-local cache invalidation without adding block-body scanning to wallet-toolbox; `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts test/messaging/CacheInvalidationPublisher.test.ts test/monitor/Monitor.parallel.test.ts test/chaintracker/SpvHeaderSync.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed.
- Final spot-check on 2026-05-16: strengthened `test/security/DependencyPolicy.test.ts` with a production-source import scan for excluded runtime packages (`bullmq`, `ioredis`, `redis`, `nats`, `better-sqlite3`, and `sqlite3`); `timeout 120s npx jest test/security/DependencyPolicy.test.ts --runInBand` passed with 1 suite and 3 tests, and `timeout 120s npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: added the named-file checklist mapping the source document's explicit `Files to modify` block to current artifacts, including source-compatible replacements where NATS runtime ownership is intentionally outside wallet-toolbox.
- Final spot-check on 2026-05-16: full post-SQLite-cleanup `timeout 1000s npm test -- --runInBand` passed: 79 suites, 579 tests, 10 skipped.
- Final spot-check on 2026-05-16: after adding stale-input UTXO invalidation coverage, `timeout 180s npm run build`, `timeout 120s npx jest test/storage/markStaleInputsAsSpent.concurrent.test.ts test/storage/attemptToPostReqsToNetwork.history.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 2 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after aligning `confirmSpendableOutputs` with the shared storage bulk-read concurrency cap and adding bounded-parallelism coverage, `timeout 180s npm run build`, `timeout 120s npx jest test/storage/confirmSpendableOutputs.test.ts test/storage/StorageProvider.bulkReads.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 6 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after wiring `UtxoCacheManager` to clear cached read hints on reorg events, `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts test/chaintracker/SpvHeaderSync.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 3 suites and 18 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after hardening RocksDB secondary index lookups against stale index records, `timeout 180s npm run build`, `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 1 suite and 9 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after exposing `WalletServicesOptions.httpClientOptions` for default pooled HTTP client sizing, `timeout 180s npm run build`, `timeout 120s npx jest test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 7 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings. `rg -n "UndiciHttpClient|undici" client/out mobile/out` returned no matches.
- Final spot-check on 2026-05-16: after exposing `MonitorOptions.broadcastConfig` for `TaskSendWaiting` chunk/concurrency tuning, `timeout 120s npx jest test/monitor/Monitor.parallel.test.ts src/monitor/tasks/__tests/TaskSendWaiting.test.ts --runInBand` and `npm run depcheck` passed; Jest reported 2 suites and 15 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding source-doc-compatible single-`txid`/`rawTx` broadcast request support, `timeout 180s npm run build`, `timeout 90s npx jest test/messaging/BroadcastConsumer.test.ts test/broadcast/BroadcastOrchestrator.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 6 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding source-doc-compatible `UtxoCacheManager.getUtxoStatus(outpoint)` and `setUtxoStatus(outpoint, isUtxo, blockHeight)` helpers, `timeout 180s npm run build`, `timeout 90s npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 14 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding source-doc-compatible `RocksDbWalletStore.updateOutput(output)`, `timeout 180s npm run build`, `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 1 suite and 8 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding source-doc-compatible `BlockHeaderCache.getHeader(height)` and `setHeader(height, header)` aliases, `timeout 180s npm run build` and `timeout 90s npx jest test/cache/CacheManagers.test.ts test/messaging/CacheInvalidationPublisher.test.ts --runInBand` passed; Jest reported 2 suites and 7 tests.
- Final spot-check on 2026-05-16: after adding source-doc-compatible `CacheInvalidationPublisher` convenience methods for block-height and outpoint-list invalidation, `timeout 180s npm run build`, `timeout 90s npx jest test/messaging/CacheInvalidationPublisher.test.ts test/cache/CacheManagers.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 7 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding dependency-cruiser rules that keep `src/messaging` in-process and away from concrete storage/monitor/service layers, `timeout 180s npm run build`, `timeout 90s npx jest test/messaging/BroadcastConsumer.test.ts test/messaging/CacheInvalidationPublisher.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 3 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding the in-process `BroadcastConsumer`, `timeout 180s npm run build`, `timeout 90s npx jest test/messaging/BroadcastConsumer.test.ts test/broadcast/BroadcastOrchestrator.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 5 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding the in-process `CacheInvalidationPublisher`, `timeout 180s npm run build`, `timeout 90s npx jest test/messaging/CacheInvalidationPublisher.test.ts test/cache/CacheManagers.test.ts --runInBand`, and `npm run depcheck` passed; Jest reported 2 suites and 7 tests, depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after strengthening the dependency policy to reject installed excluded runtime packages in `package-lock.json`, `timeout 180s npm run build`, `timeout 90s npx jest test/security/DependencyPolicy.test.ts --runInBand`, and `npm run depcheck` passed; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after exposing the RocksDB binding-supported read-only, stats, and transaction-log options, `timeout 180s npm run build` and `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: after adding `RocksDbWalletStore.prepareForFilesystemSnapshot()`, `npm run build` and `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: current non-SQLite architecture matrix passed with `timeout 300s npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts test/broadcast/BroadcastOrchestrator.test.ts test/services/Services.postBeefTimeouts.test.ts test/storage/RocksDbWalletStore.test.ts test/storage/StorageProvider.bulkReads.test.ts test/storage/attemptToPostReqsToNetwork.history.test.ts src/monitor/tasks/__tests/TaskSendWaiting.test.ts src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts test/metrics/WalletToolboxMetrics.test.ts test/metrics/PrometheusAlerts.test.ts test/chaintracker/SpvHeaderSync.test.ts test/monitor/Monitor.parallel.test.ts src/monitor/tasks/__tests/TaskArcSSE.test.ts test/security/DependencyPolicy.test.ts test/security/HttpClientPolicy.test.ts test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts --runInBand`: 18 suites, 89 tests.
- Final spot-check on 2026-05-16: current post-SQLite-cleanup `npm run loadtest:single-instance` passed with 1000 calls, concurrency 100, `maxActive` 100, and about 26.1k mocked TPS; `npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: expanded `docs/nektar-runtime-handoff.md` with JetStream subject, payload, idempotency, wallet-adapter mapping, consumer-setting, stream-retention, TLS/credential, NATS latency/backlog/redelivery metric, alert, persistence-test, runtime task-coordination, and disaster-recovery contracts for the Nektar-owned NATS streams.
- Final spot-check on 2026-05-16: after deleting SQLite-backed tests/helpers/fixtures and rebuilding `out/` from a clean directory, `npm run build`, `npm run depcheck`, and `timeout 90s npx jest test/security/DependencyPolicy.test.ts test/storage/markStaleInputsAsSpent.concurrent.test.ts --runInBand` passed; depcheck reported 0 errors and the same 15 existing circular-dependency warnings. `npm ls better-sqlite3 sqlite3 --depth=0` showed no installed packages, `find . -path './node_modules' -prune -o -type f \( -iname '*.sqlite' -o -iname '*.sqlite-*' -o -iname '*sqlite*' \) -print` returned no files, and repo grep found SQLite strings only in intentional dependency-ban policy/audit entries.
- Final spot-check on 2026-05-16: after removing the production SQLite runtime surface, `npm run build`, `npm run depcheck`, and `timeout 90s npx jest test/security/DependencyPolicy.test.ts test/storage/markStaleInputsAsSpent.concurrent.test.ts --runInBand` passed; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after updating public docs away from the removed SQLite setup APIs, `rg -n "createWalletSQLite|createSQLiteKnex|SetupWalletSQLite|sqliteFilename|SQLite|sqlite" README.md docs/README.md docs/wab-shamir.md docs/setup.md docs/storage.md docs/client.md docs/wallet.md docs/monitor.md docs/services.md` returned no matches.
- Final spot-check on 2026-05-16: after replacing the stale-input concurrency coverage with a SQLite-free in-memory storage fake, `npm run build` and `timeout 90s npx jest test/storage/markStaleInputsAsSpent.concurrent.test.ts --runInBand` passed: 1 test.
- Final spot-check on 2026-05-16: `npm run build` passed.
- Final spot-check on 2026-05-16: after adding direct UTXO and block-header cache hit-rate gauges, `npm run build` passed.
- Final spot-check on 2026-05-16: after adding direct cache hit-rate gauges, `npx jest test/metrics/WalletToolboxMetrics.test.ts test/metrics/PrometheusAlerts.test.ts test/cache/CacheManagers.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: after adding direct cache hit-rate gauges, `npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after extending `/metrics` endpoint assertions for direct cache hit-rate gauges, `npm run build` and `npx jest test/storage/adminServer/AdminServer.metrics.test.ts test/metrics/WalletToolboxMetrics.test.ts --runInBand` passed: 2 tests.
- Final spot-check on 2026-05-16: after codifying that cache hit-rate alerts use recent-window request counters rather than process-lifetime gauges, `npm run build`, `npm run depcheck`, and `npx jest test/metrics/PrometheusAlerts.test.ts test/metrics/WalletToolboxMetrics.test.ts test/storage/adminServer/AdminServer.metrics.test.ts --runInBand` passed: 3 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: rechecked provider HTTP migration coverage after auditing `fundWalletP2PKH.ts`; `npm run build` and `npx jest test/fundWalletP2PKH.test.ts test/security/HttpClientPolicy.test.ts test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: rechecked `TaskSendWaiting` default cadence, chunk size, bounded batch expansion, retry priority, and queue metric coverage; `npm run build` and `npx jest src/monitor/tasks/__tests/TaskSendWaiting.test.ts src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts --runInBand` passed: 10 tests.
- Final spot-check on 2026-05-16: after strengthening the single-instance load harness to verify bounded parallelism and queue close, `npm run loadtest:single-instance` passed with 1000 calls, concurrency 100, `maxActive` 100, and about 51.9k mocked TPS.
- Final spot-check on 2026-05-16: after strengthening the load harness, `npx jest test/broadcast/BroadcastOrchestrator.test.ts test/services/Services.postBeefTimeouts.test.ts --runInBand` passed: 11 tests, and `npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: source-document sections for API/config, service-provider HTTP shape, rate limiting, storage query metrics, and disaster recovery were re-audited against current code artifacts.
- Final spot-check on 2026-05-16: `npx jest test/security/RateLimiter.test.ts test/storage/remoting/StorageServer.rateLimit.test.ts --runInBand` passed: 6 tests.
- Final spot-check on 2026-05-16: `npx jest test/metrics/WalletToolboxMetrics.test.ts test/storage/RocksDbWalletStore.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: after adding service-level coverage that both `getUtxoStatus(..., useNext=true)` and `isUtxo(output, true)` bypass UTXO cache hints, `npx jest test/services/Services.cache.test.ts --runInBand` passed: 7 tests.
- Final spot-check on 2026-05-16: after adding service-level `useNext` cache-bypass coverage, `npm run build` and `npm run depcheck` passed; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after aligning `BlockHeaderCache.getStats()` with UTXO/script-hash cache hit-rate stats, `npm run build` and `npx jest test/cache/CacheManagers.test.ts --runInBand` passed: 6 tests.
- Final spot-check on 2026-05-16: `npx jest test/cache/CacheManagers.test.ts test/metrics/WalletToolboxMetrics.test.ts test/metrics/PrometheusAlerts.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: after the cache stats change, `npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: after adding `BroadcastOrchestrator.close()`, `npm run build`, `npm run depcheck`, and `npx jest test/broadcast/BroadcastOrchestrator.test.ts test/services/Services.postBeefTimeouts.test.ts --runInBand` passed: 11 tests; depcheck reported 0 errors and 15 existing warnings.
- Final spot-check on 2026-05-16: after adding the block-header cache hit-rate alert, `npm run build` and `npx jest test/metrics/PrometheusAlerts.test.ts test/metrics/WalletToolboxMetrics.test.ts test/cache/CacheManagers.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: after expanding `/metrics` endpoint assertions for block-header cache metrics, `npm run build` and `npx jest test/storage/adminServer/AdminServer.metrics.test.ts test/metrics/PrometheusAlerts.test.ts test/metrics/WalletToolboxMetrics.test.ts --runInBand` passed: 3 tests.
- Final spot-check on 2026-05-16: after adding block-event-to-cache integration coverage, `npm run build` and `npx jest test/monitor/Monitor.parallel.test.ts test/cache/CacheManagers.test.ts test/chaintracker/SpvHeaderSync.test.ts --runInBand` passed: 15 tests.
- Final spot-check on 2026-05-16: after the block-event-to-cache integration coverage, `npm run depcheck` passed with 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: `npm run build && node -e ...` verified built package exports for `UtxoCacheManager`, `BlockHeaderCache`, `ScriptHashCache`, `EventBus`, `UndiciHttpClient`, `BroadcastOrchestrator`, `WalletToolboxMetrics`, `PrometheusMetrics`, and `SpvHeaderSync`.
- Final spot-check on 2026-05-16: after `npm run build`, `require('./client/out/src/index.client.js')` and `require('./mobile/out/src/index.mobile.js')` loaded successfully, and `rg` found no `undici`/`UndiciHttpClient` references in generated client/mobile output.
- Final spot-check on 2026-05-16: inspected `@harperfast/rocksdb-js` type declarations and README; the binding exposes `parallelismThreads`, `disableWAL`, `enableStats`, `noBlockCache`, `pessimistic`, `readOnly`, `statsLevel`, and transaction-log options, but not the source document's requested `writeBufferSize`.
- Final spot-check on 2026-05-16: `npm run depcheck` passed with 0 errors and the 15 existing circular-dependency warnings listed below.
- Final spot-check on 2026-05-16: the CI `depcheck:ci` shell block from `.github/workflows/push.yaml` passed locally and printed `{ "errors": 0, "warnings": 15, "totalCruised": 264, "totalDependenciesCruised": 777 }`.
- Final spot-check on 2026-05-16: `npm run depcheck:graph && npm run depcheck:html` passed; generated dependency-cruiser report files are ignored by `.gitignore`.
- Final spot-check on 2026-05-16: `npx jest test/security/DependencyPolicy.test.ts test/security/HttpClientPolicy.test.ts test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts --runInBand` passed: 8 tests.
- Final spot-check on 2026-05-16: `npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts test/broadcast/BroadcastOrchestrator.test.ts test/services/Services.postBeefTimeouts.test.ts --runInBand` passed: 21 tests.
- Final spot-check on 2026-05-16: `npx jest test/storage/RocksDbWalletStore.test.ts test/storage/StorageProvider.bulkReads.test.ts test/storage/attemptToPostReqsToNetwork.history.test.ts --runInBand` passed: 12 tests.
- Final spot-check on 2026-05-16: `npx jest src/monitor/tasks/__tests/TaskSendWaiting.test.ts src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts test/metrics/WalletToolboxMetrics.test.ts test/metrics/PrometheusAlerts.test.ts --runInBand` passed: 12 tests.
- Final spot-check on 2026-05-16: `npx jest test/chaintracker/SpvHeaderSync.test.ts test/monitor/Monitor.parallel.test.ts src/monitor/tasks/__tests/TaskArcSSE.test.ts --runInBand` passed: 32 tests.
- Final spot-check on 2026-05-16: `npm run loadtest:single-instance` passed: 1000 mocked calls, concurrency 100, observed about 35.8k TPS before the load harness was strengthened with peak-active verification.
- Prior verification: `npm run build` passed.
- `npx jest test/storage/RocksDbWalletStore.test.ts --runInBand` passed: 7 tests.
- `npx jest test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts --runInBand` passed: 6 tests.
- `npx jest test/cache/CacheManagers.test.ts --runInBand` passed: 6 tests.
- `npx jest test/services/Services.cache.test.ts --runInBand` passed: 5 tests.
- `npx jest test/storage/adminServer/AdminServer.metrics.test.ts --runInBand` passed: 1 test.
- `npx jest src/monitor/tasks/__tests/TaskArcSSE.test.ts test/monitor/Monitor.parallel.test.ts --runInBand` passed: 29 tests.
- `npx jest src/monitor/tasks/__tests__/TaskUnFail.test.ts test/storage/ListOutputsSpecOp.test.ts test/services/Services.cache.test.ts --runInBand` passed: 6 tests.
- `npx jest test/storage/ListOutputsSpecOp.test.ts src/monitor/tasks/__tests__/TaskUnFail.test.ts --runInBand` passed before SQLite test cleanup; stale-input concurrency coverage now lives in `test/storage/markStaleInputsAsSpent.concurrent.test.ts`.
- `npx jest test/storage/confirmSpendableOutputs.test.ts test/storage/ListOutputsSpecOp.test.ts src/monitor/tasks/__tests__/TaskUnFail.test.ts --runInBand` passed before SQLite test cleanup; stale-input concurrency coverage now lives in `test/storage/markStaleInputsAsSpent.concurrent.test.ts`.
- `npx jest test/storage/StorageProvider.bulkReads.test.ts --runInBand` passed: 4 tests.
- `npx jest test/storage/StorageProvider.getReqsAndBeef.test.ts --runInBand` passed: 1 test.
- `npx jest test/chaintracker/SpvHeaderSync.test.ts test/monitor/Monitor.parallel.test.ts --runInBand` passed: 7 tests.
- `npx jest src/monitor/tasks/__tests/TaskSendWaiting.test.ts src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts --runInBand` passed: 10 tests.
- `npx jest test/broadcast/BroadcastOrchestrator.test.ts --runInBand` passed: 2 tests.
- `npx jest src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts src/monitor/tasks/__tests/TaskSendWaiting.test.ts test/broadcast/BroadcastOrchestrator.test.ts --runInBand` passed: 10 tests.
- `npx jest test/metrics/WalletToolboxMetrics.test.ts test/metrics/PrometheusAlerts.test.ts src/monitor/tasks/__tests/TaskSendWaiting.processUnsent.test.ts --runInBand` passed: 4 tests.
- `npx jest test/Wallet/construct/Wallet.destroy.lifecycle.test.ts test/monitor/Monitor.parallel.test.ts --runInBand` passed: 5 tests.
- `npx jest test/services/ProviderHttpClientDefaults.test.ts test/services/UndiciHttpClient.test.ts test/Wallet/construct/Wallet.destroy.lifecycle.test.ts --runInBand` passed: 7 tests.
- `npx jest test/services/Services.postBeefTimeouts.test.ts --runInBand` passed: 8 tests.
- `npx jest test/storage/attemptToPostReqsToNetwork.history.test.ts test/services/Services.postBeefTimeouts.test.ts --runInBand` passed: 7 tests.
- `npx jest test/storage/attemptToPostReqsToNetwork.history.test.ts --runInBand` passed before SQLite test cleanup; stale-input concurrency coverage now lives in `test/storage/markStaleInputsAsSpent.concurrent.test.ts`.
- `npx jest test/security/DependencyPolicy.test.ts test/security/HttpClientPolicy.test.ts --runInBand` passed before SQLite test cleanup.
- `npm run depcheck` passed with 0 errors and 15 existing circular-dependency warnings.
- `npm run loadtest:single-instance` passed: 1000 mocked calls, target 1000 TPS, observed about 73k TPS before the current peak-active verification was added.
- Before SQLite test cleanup, full Jest was blocked by slow legacy SQLite-backed suites. Those suites have since been deleted with the SQLite runtime surface.
- Final non-lint verification on 2026-05-16: `timeout 180s npm run build`, `timeout 120s npm run depcheck`, `timeout 180s npx jest test/security/DependencyPolicy.test.ts test/cache/CacheManagers.test.ts test/storage/RocksDbWalletStore.test.ts test/monitor/Monitor.parallel.test.ts --runInBand`, `git diff --check`, `npm ls better-sqlite3 sqlite3 --depth=0`, and a SQLite fixture/file scan all passed. Depcheck reported 0 errors and the same 15 existing circular-dependency warnings; Jest reported 4 suites and 37 tests.
- Final spot-check on 2026-05-16: tightened `findOutputsByOutpoints` so stale user-scoped outpoint index records cannot return outputs now owned by another user; `timeout 180s npm run build`, `timeout 120s npx jest test/storage/RocksDbWalletStore.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 11 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: hardened `SpvHeaderSync.start()` so a partial subscription startup failure unsubscribes any created subscription and clears the failed start promise for retry; `timeout 180s npm run build`, `timeout 120s npx jest test/chaintracker/SpvHeaderSync.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 5 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: hardened `SpvHeaderSync.stop()` so it waits for any in-flight startup before unsubscribing, preventing subscription leaks when shutdown races startup; `timeout 180s npm run build`, `timeout 120s npx jest test/chaintracker/SpvHeaderSync.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 6 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: tightened `UtxoCacheManager` so it only retains successful provider responses with a decisive boolean `isUtxo` value, preventing incomplete provider successes from becoming cached read hints; `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 22 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: tightened Services block-header caching so headers returned by chaintrackers or fallback providers must match the requested height/hash before cache insertion; `timeout 180s npm run build`, `timeout 120s npx jest test/services/Services.cache.test.ts test/cache/CacheManagers.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 23 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: tightened `BlockHeaderCache.setHeader(height, header)` so it rejects mismatched height metadata instead of rewriting the header before caching; `timeout 180s npm run build`, `timeout 120s npx jest test/cache/CacheManagers.test.ts test/services/Services.cache.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 24 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: tightened `BroadcastOrchestrator` and the in-process `BroadcastConsumer` adapter boundary so a request cannot specify both the internal `txids` array and source-doc single `txid` shape, preventing ambiguous provider broadcast inputs before queueing; `timeout 180s npm run build`, `timeout 120s npx jest test/broadcast/BroadcastOrchestrator.test.ts test/messaging/BroadcastConsumer.test.ts --runInBand`, `timeout 120s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 11 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: clarified the RocksDB disaster-recovery handoff in `docs/rocksdb.md` so operators quiesce writes or use crash-consistent volume snapshots, call `prepareForFilesystemSnapshot({ compact: true })`, snapshot the returned path, and rehearse restore in staging; `git diff --check` passed.
- Final spot-check on 2026-05-16: tightened `BroadcastOrchestrator` so the normalized txid list is copied before work is queued, preventing caller-side mutation from changing provider inputs after validation; `timeout 300s npm run build`, `timeout 240s npx jest test/broadcast/BroadcastOrchestrator.test.ts test/messaging/BroadcastConsumer.test.ts --runInBand`, `timeout 180s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 12 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: tightened broadcast txid validation so blank and whitespace-padded txids are rejected before provider dispatch; `timeout 300s npm run build`, `timeout 240s npx jest test/broadcast/BroadcastOrchestrator.test.ts test/messaging/BroadcastConsumer.test.ts --runInBand`, `timeout 180s npm run depcheck`, and `git diff --check` passed. Jest reported 2 suites and 13 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: strengthened the Nektar handoff contract drift guard so runtime-only stream settings, TLS/security guidance, object-storage restore guidance, staged throughput gates, public-network validation checks, and no-cache-authority/no-replacement-transaction boundary rules are covered; `timeout 240s npx jest test/docs/NektarRuntimeHandoff.test.ts --runInBand`, `timeout 300s npm run build`, `timeout 180s npm run depcheck`, and `git diff --check` passed. Jest reported 1 suite and 3 tests; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.
- Final spot-check on 2026-05-16: expanded the named-file checklist to map source code-block filenames individually, including `src/messaging/NatsManager.ts` as a Nektar-owned exclusion rather than an unimplemented wallet-toolbox file; `git diff --check` passed.
- Final spot-check on 2026-05-16: fact-checked the practical-testing review. `src/messaging` exists and is exported, `npm ls better-sqlite3 sqlite3 --depth=0` showed `(empty)`, the mocked load harness now emits `mode: "mocked-in-process"` and lists uncovered real I/O, and `docs/practical-testing-plan.md` records the testnet ramp, observability, bottleneck, reorg, cache-safety, and Nektar-adapter checks. `timeout 300s npm run build`, `timeout 240s npx jest --runTestsByPath test/docs/PracticalTestingPlan.test.ts test/docs/NektarRuntimeHandoff.test.ts --runInBand`, `timeout 300s npx jest --runTestsByPath test/broadcast/BroadcastOrchestrator.test.ts --runInBand --testNamePattern "uses attempts as retry priority"`, `timeout 120s node out/test/performance/SingleInstanceThroughput.load.js`, `timeout 180s npm run depcheck`, and `git diff --check` passed. Jest reported 2 docs suites/6 tests plus 1 focused broadcast test; depcheck reported 0 errors and the same 15 existing circular-dependency warnings.

## Residual Gaps

- Full source-document NATS JetStream architecture is not implemented here by design.
- First-party SQLite references are now limited to intentional dependency-ban policy/audit entries. `package-lock.json` still contains `better-sqlite3`/`sqlite3` only as optional Knex peer metadata, not as installed root dependencies.
- NATS S3 snapshots and NATS disaster-recovery operations are not implemented here by design.
- RocksDB point-in-time backup/snapshot automation remains operator-owned; the library can flush/compact and prepare a filesystem-snapshot handoff, but the binding does not expose a checkpoint/backup API suitable for safe online backups.
- Public-network 1000 tx/s finality is not proven by the mocked load harness; `docs/practical-testing-plan.md` defines the required real-network testnet phase.
- `npm run lint --if-present` is not clean because of existing legacy lint violations outside this focused change set.
